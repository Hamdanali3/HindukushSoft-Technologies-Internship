@extends('layouts.app') @section('content')@if(session('success'))<div class="alert success">{{ session('success') }}</div>@endif<section class="hero"><div class="eyebrow">Task detail</div><h1>{{ $task->title }}</h1><p class="lead">{{ $task->description ?: 'No description was added.' }}</p></section><section class="grid"><div class="card"><strong>Status</strong><p>{{ str_replace('_',' ',ucfirst($task->status)) }}</p></div><div class="card"><strong>Priority</strong><p>{{ ucfirst($task->priority) }}</p></div><div class="card"><strong>Due</strong><p>{{ $task->due_date?->format('M d, Y')??'—' }}</p></div></section><a class="btn" href="{{ route('tasks.edit',$task) }}">Edit task</a> <a class="btn secondary" href="{{ route('tasks.index') }}">Back</a>@endsection
