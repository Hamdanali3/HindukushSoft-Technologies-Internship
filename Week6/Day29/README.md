# Day 29 — Workboard CRUD

Laravel 12 task management with resource routing, Form Request validation, search, status filtering, pagination, flash messages and safe deletion.

## Setup
`composer install`
`cp .env.example .env`
`php artisan key:generate`
`php artisan migrate --seed`
`php artisan serve`
