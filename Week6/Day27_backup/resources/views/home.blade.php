@extends('layouts.app') @section('content')<section class="hero"><div class="eyebrow">Laravel · Routing & Controllers</div><h1>A clear route map keeps request flow understandable.</h1><p class="lead">Project Pulse demonstrates named routes, route groups, route parameters, controller actions and reusable Blade layouts.</p></section><section class="grid">@foreach($stats as $stat)<article class="card"><div class="metric">{{ $stat['value'] }}</div><div class="muted">{{ $stat['label'] }}</div></article>@endforeach</section><section class="card" style="margin-top:18px"><h2>Controller-driven project pages</h2><p class="muted">The project route accepts a slug and the controller resolves it to structured data.</p><a class="btn" href="{{ route('projects.show','workboard') }}">Open Workboard</a></section>@endsection
