# Day 27 — Project Pulse

Laravel 12 routing/controller showcase.

## Setup
`composer install`
`cp .env.example .env`
`php artisan key:generate`
`php artisan serve`

## Focus
Named routes, route groups, route parameters, controller actions and reusable Blade layouts.
