@extends('layouts.auth') @section('content')<section class="hero"><div class="eyebrow">Secure workspace</div><h1>Welcome back.</h1><p class="lead">Sign in to continue to your private dashboard.</p></section>@if(session('success'))<div class="alert success">{{ session('success') }}</div>@endif<form method="post" action="{{ route('login.store') }}">@csrf<div class="field"><label>Email</label><input type="email" name="email" value="{{ old('email') }}" required autocomplete="email">@error('email')<div class="error">{{ $message }}</div>@enderror</div><div class="field"><label>Password</label><input type="password" name="password" required autocomplete="current-password">@error('password')<div class="error">{{ $message }}</div>@enderror</div><label><input style="width:auto" type="checkbox" name="remember" value="1"> Remember me</label><br><button class="btn" style="margin-top:15px;width:100%" type="submit">Sign in</button></form><p class="muted">New here? <a href="{{ route('register') }}">Create an account</a></p>@endsection
